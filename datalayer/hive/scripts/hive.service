[Unit]
Description=Hive systemd service

[Service]
Type=simple
User=hadoop
Group=hadoop
WorkingDirectory=/opt/hive
ExecStart=/opt/hive/bin/hive --service hiveserver2 --hiveconf hive.server2.transport.mode=binary --hiveconf hive.server2.thrift.bind.host=JP-DTP-ELK-VM --hiveconf hive.server2.thrift.port=10000 
TimeoutSec=30
Restart=on-failure
RestartSec=30
StartLimitInterval=350
StartLimitBurst=10

[Install]
WantedBy=multi-user.target

